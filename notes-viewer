/* ================================================================
   notes-viewer.css  —  Add-on styles for Note Viewer Modal
   Link AFTER style.css in index.html
   ================================================================================ */

/* ── Panel top bar (edit area header row) ──────────────────── */
.panel-top-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 6px;
}
.panel-top-bar .area-label { margin-bottom: 0; }

.view-rendered-btn {
  background: rgba(99,179,237,0.12);
  border: 1px solid rgba(99,179,237,0.3);
  color: #63b3ed;
  border-radius: 6px;
  padding: 3px 10px;
  font-size: 0.78rem;
  cursor: pointer;
  transition: background 0.2s, color 0.2s;
  white-space: nowrap;
}
.view-rendered-btn:hover {
  background: rgba(99,179,237,0.22);
  color: #90cdf4;
}

/* Notes button has-notes indicator */
.notes-btn.has-notes {
  position: relative;
}
.notes-btn.has-notes::after {
  content: '';
  position: absolute;
  top: 2px; right: 2px;
  width: 7px; height: 7px;
  background: #00d4aa;
  border-radius: 50%;
  border: 1px solid var(--bg-card, #1a1a2e);
}

/* ── Note Viewer Modal overlay ─────────────────────────────── */
.note-viewer-modal {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.75);
  backdrop-filter: blur(4px);
  z-index: 1000;
  display: none;
  align-items: center;
  justify-content: center;
  padding: 16px;
  box-sizing: border-box;
}

/* 1. Updated .note-viewer-inner */
.note-viewer-inner {
  background: rgba(17,25,40,.95);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 24px;
  width: 95vw;
  max-width: 1300px;
  max-height: 92vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow:
    0 20px 80px rgba(0,0,0,.7),
    0 0 0 1px rgba(99,179,237,.1);
  animation: nvSlideIn 0.22s ease;
}
@keyframes nvSlideIn {
  from { opacity:0; transform:translateY(24px) scale(0.97); }
  to   { opacity:1; transform:translateY(0) scale(1); }
}

/* ── Viewer Header ─────────────────────────────────────────── */
/* 2. Premium header */
.nv-header {
  padding: 24px 30px;
  border-bottom: 1px solid rgba(255,255,255,.08);
  background:
    linear-gradient(
      180deg,
      rgba(99,179,237,.08),
      transparent
    );
  display: flex;
  flex-direction: column;
  gap: 6px;
  position: relative;
  flex-shrink: 0;
}
.nv-header-left {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}
.nv-q-num {
  font-size: 0.75rem;
  color: var(--text-muted, #7a8bb5);
  font-family: monospace;
  background: rgba(255,255,255,0.06);
  padding: 2px 7px;
  border-radius: 4px;
}
.nv-topic-badge {
  font-size: 0.72rem;
  font-weight: 700;
  color: #63b3ed;
  background: rgba(99,179,237,0.12);
  padding: 2px 8px;
  border-radius: 4px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.nv-diff-badge {
  font-size: 0.68rem;
  font-weight: 700;
  padding: 2px 8px;
  border-radius: 4px;
}
/* reuse q-badge color vars */
.nv-diff-badge.Easy   { background: rgba(72,187,120,0.2); color: #48bb78; }
.nv-diff-badge.Medium { background: rgba(237,137,54,0.2);  color: #ed8936; }
.nv-diff-badge.Hard   { background: rgba(245,101,101,0.2); color: #f56565; }

/* 3. Title */
.nv-title {
  font-size: 1.8rem;
  font-weight: 800;
  color: white;
  line-height: 1.4;
  margin-top: 10px;
  margin: 0;
  padding-right: 36px;
}
.nv-close-btn {
  position: absolute;
  top: 14px; right: 16px;
  background: rgba(255,255,255,0.07);
  border: none;
  color: var(--text-muted, #7a8bb5);
  width: 30px; height: 30px;
  border-radius: 50%;
  cursor: pointer;
  font-size: 0.9rem;
  display: flex; align-items: center; justify-content: center;
  transition: background 0.2s, color 0.2s;
}
.nv-close-btn:hover { background: rgba(245,101,101,0.2); color: #f56565; }

/* ── Viewer Tabs ───────────────────────────────────────────── */
/* 4. Tabs */
.nv-tabs {
  padding: 16px 24px;
  gap: 10px;
  display: flex;
  border-bottom: 1px solid rgba(255,255,255,0.07);
  flex-shrink: 0;
  flex-wrap: wrap;
}
.nv-tab {
  border: none;
  background: rgba(255,255,255,.05);
  color: #9aa4b2;
  border-radius: 12px;
  padding: 10px 18px;
  transition: .3s;
  cursor: pointer;
  font-size: 0.82rem;
  font-weight: 600;
  margin-bottom: -1px;
}
.nv-tab:hover { color: var(--text-primary, #e2e8f0); }
.nv-tab.active {
  background: rgba(99,179,237,.15);
  color: #63b3ed;
  border: 1px solid rgba(99,179,237,.3);
}

/* ── Viewer Body (scrollable content area) ─────────────────── */
/* 5. Body */
.nv-body {
  flex: 1;
  overflow-y: auto;
  padding: 35px;
  font-size: 16px;
  line-height: 1.9;
  scrollbar-width: thin;
  scrollbar-color: rgba(99,179,237,0.3) transparent;
}
.nv-body::-webkit-scrollbar { width: 5px; }
.nv-body::-webkit-scrollbar-thumb { background: rgba(99,179,237,0.3); border-radius: 3px; }

/* ── Markdown Rendered Styles ──────────────────────────────── */
/* 6. Markdown */
.nv-markdown-body {
  max-width: 950px;
  margin: auto;
  color: #e5e7eb;
  font-size: 16px;
  line-height: 1.75;
}
.md-empty { color: var(--text-muted, #7a8bb5); font-style: italic; }

/* 7. Headings */
.md-h1 {
  font-size: 2rem;
  color: #58a6ff;
  margin-bottom: 20px;
  border-bottom: 2px solid rgba(88,166,255,.2);
  padding-bottom: 10px;
  font-weight: 700;
  line-height: 1.3;
}
.md-h2 {
  font-size: 1.5rem;
  color: #79c0ff;
  margin-top: 30px;
  font-weight: 700;
  line-height: 1.3;
}
.md-h3 {
  font-size: 1.2rem;
  color: #a5d6ff;
  font-weight: 700;
  line-height: 1.3;
}
.md-h4 { font-size: 0.98rem; color: #76c9d8; font-weight: 700; line-height: 1.3; }
.md-h5,.md-h6 { font-size: 0.9rem; color: #7a8bb5; font-weight: 700; line-height: 1.3; }

.md-p {
  margin: 0.55em 0;
}

.md-bold   { font-weight: 700; color: #f6e05e; }
.md-italic { font-style: italic; color: #d6bcfa; }
.md-del    { text-decoration: line-through; color: var(--text-muted, #7a8bb5); }
.md-mark {
  background: rgba(246,224,94,0.2);
  color: #f6e05e;
  padding: 1px 4px;
  border-radius: 3px;
}

/* 9. Inline Code */
.md-inline-code {
  background: rgba(88,166,255,.15);
  color: #58a6ff;
  padding: 3px 8px;
  border-radius: 6px;
  font-weight: 600;
  font-family: 'Fira Code', 'Cascadia Code', 'Consolas', monospace;
  font-size: 0.85em;
  border: 1px solid rgba(99,179,237,0.2);
}

/* 8. Code Block (Most Important) */
.md-code-block {
  background: #0d1117;
  border: 1px solid #30363d;
  border-radius: 16px;
  padding: 24px;
  margin: 25px 0;
  overflow-x: auto;
  box-shadow:
    0 12px 40px rgba(0,0,0,.4);
  position: relative;
}
.md-code-block::before {
  content: attr(data-lang);
  position: absolute;
  top: 8px; right: 12px;
  font-size: 0.68rem;
  color: #4a5568;
  text-transform: uppercase;
  font-family: monospace;
}
.md-code-block code {
  font-family:
    "JetBrains Mono",
    "Fira Code",
    monospace;
  font-size: 15px;
  line-height: 1.8;
  color: #c9d1d9;
  white-space: pre;
}
/* Syntax highlight hints */
.md-code-block .lang-java code,
.md-code-block .lang-python code,
.md-code-block .lang-cpp code,
.md-code-block .lang-js code,
.md-code-block .lang-javascript code { color: #a8d8a8; }

.nv-code-full {
  margin: 0;
  border-radius: 10px;
  min-height: 120px;
}
.nv-code-wrap { padding: 2px; }

/* 10. Blockquote */
.md-blockquote {
  background: rgba(99,179,237,.08);
  border-left: 4px solid #63b3ed;
  padding: 16px;
  border-radius: 10px;
  margin: 18px 0;
  color: #d1d5db;
  font-style: italic;
}

.md-hr {
  border: none;
  border-top: 1px solid rgba(255,255,255,0.12);
  margin: 16px 0;
}

/* 11. Lists */
.md-ul, .md-ol {
  padding-left: 1.5em;
  margin: 8px 0;
}
.md-ul li, .md-ol li {
  margin: 10px 0;
  color: var(--text-primary, #e2e8f0);
}
.md-ul li::marker { color: #63b3ed; }
.md-ol li::marker { color: #63b3ed; font-weight: 700; }

.md-task {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 5px 0;
  cursor: default;
  font-size: 0.9rem;
}
.md-task input { accent-color: #63b3ed; width: 15px; height: 15px; cursor: default; }
.md-task.done { text-decoration: line-through; color: var(--text-muted, #7a8bb5); }

/* ── Image Grid in Viewer ──────────────────────────────────── */
.nv-image-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 14px;
  padding: 4px;
}
.nv-img-wrap {
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 10px;
  overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s;
}
.nv-img-wrap:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.4);
}
.nv-img-wrap img {
  width: 100%;
  display: block;
  cursor: zoom-in;
  transition: transform 0.25s;
  max-height: 220px;
  object-fit: cover;
}
.nv-img-wrap img.nv-img-zoom {
  max-height: none;
  transform: scale(1);
  cursor: zoom-out;
  object-fit: contain;
  background: #000;
}
.nv-img-name {
  font-size: 0.72rem;
  color: var(--text-muted, #7a8bb5);
  padding: 6px 10px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  border-top: 1px solid rgba(255,255,255,0.06);
}

/* ── Viewer Footer ─────────────────────────────────────────── */
/* 12. Footer */
.nv-footer {
  padding: 18px 24px;
  background: rgba(255,255,255,.02);
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-top: 1px solid rgba(255,255,255,0.07);
  flex-shrink: 0;
  flex-wrap: wrap;
  gap: 8px;
}
.nv-saved-time {
  font-size: 0.75rem;
  color: var(--text-muted, #7a8bb5);
}
.nv-footer-actions { display: flex; gap: 8px; }

/* ── Responsive ────────────────────────────────────────────── */
@media (max-width: 600px) {
  .note-viewer-inner { border-radius: 12px; max-height: 95vh; width: 100vw; }
  .nv-body { padding: 14px 14px; }
  .nv-header { padding: 16px 18px; }
  .nv-tabs { padding: 12px 18px; }
  .nv-image-grid { grid-template-columns: 1fr 1fr; }
  .nv-title { font-size: 1.2rem; }
  .md-h1 { font-size: 1.6rem; }
  .md-h2 { font-size: 1.3rem; }
  .md-h3 { font-size: 1.1rem; }
}
